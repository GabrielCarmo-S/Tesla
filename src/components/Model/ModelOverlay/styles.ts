import styled from "styled-components";

export const Container = styled.div`
  position: sticky;
  top: 0;
  height: 10vh;
  margin-top: -100vh;

  display: flex;
  flex-direction: column;
  align-items: center;
`;
